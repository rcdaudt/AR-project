$ roslaunch ar_project_clean gazebo.launch
$ roslaunch ar_project_clean driver.launch x:=200 y:=350

# coordinates are given in image pixels
# when the robot reaches the goal,
# you can run driver.launch with different coordinates without turning gazebo off
# e.g.:

$ roslaunch ar_project_clean driver.launch x:=200 y:=200
